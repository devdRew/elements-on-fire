// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBiWc6X969Y2Zng1G5y1nsZQRMF71aFn-o",
    authDomain: "elements-on-fire.firebaseapp.com",
    databaseURL: "https://elements-on-fire.firebaseio.com",
    projectId: "elements-on-fire",
    storageBucket: "elements-on-fire.appspot.com",
    messagingSenderId: "970305915363",
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
